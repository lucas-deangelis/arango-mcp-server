# Contains the data generated by generate-ecommerce-data.cjs and generate-library-data.cjs.
# They're not comitted since they're almost reproducible ("almost" because the timestamps fields vary)
data/